class Solution(object):
    def isPalindrome(self, x):
        k = (str(x))[::-1]
        
        if k[-1]=='-':
            return 0
        else:
            if  int(k)!=x:
                return 0
            else:
                return 1
        """
        :type x: int
        :rtype: bool
        """
        
